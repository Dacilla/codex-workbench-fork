# Codex Workbench — agent handoff

Open `01-architecture-and-implementation.md` for the project spec. Give `02-agent-brief-cli.md` to the CLI implementation agent and `03-agent-brief-vscode.md` to the VS Code implementation agent; provide both agents `01-architecture-and-implementation.md` and `04-source-and-validation-notes.md`. The two implementations are independently releasable but should pin the same upstream Codex SHA.

This handoff is a detailed design and implementation assignment, not a finished fork, built executable, published npm package or installable VSIX.
